# معماری تجاری پیمان‌یار

پیمان‌یار یک محصول واحد با دو پلن است؛ داده‌ها هنگام ارتقا جابه‌جا نمی‌شوند.

## پلن‌ها

- **حرفه‌ای:** فرد یا تیم کوچک، حداکثر سه عضو، پروژه/سفارش، مالی، اسناد، گزارش و هوش مصنوعی.
- **سازمانی:** اعضای چندنقشی، مجوز پروژه‌ای، گردش تأیید، حسابرسی و داشبورد تجمیعی.

## قالب‌های شغلی

هسته داده مشترک است و فقط اصطلاحات، مراحل و ماژول‌های پیشنهادی براساس فعالیت تغییر می‌کنند: مدیریت پیمان، کابینت‌سازی، معماری، بازسازی، تأسیسات و کسب‌وکار عمومی پروژه‌ای.

## مرزهای معماری

1. `workspace-engine.js` مدل محلی و مهاجرت سازگار با داده‌های فعلی را مدیریت می‌کند.
2. Supabase مسئول احراز هویت، Postgres، Storage و جداسازی داده شرکت‌ها با RLS خواهد بود.
3. Cloudflare Worker واسط امن سرویس‌های هوش مصنوعی است و کلیدها هرگز به مرورگر ارسال نمی‌شوند.
4. هر رکورد تجاری دارای `workspace_id`، سازنده، زمان ایجاد و در صورت نیاز وضعیت تأیید است.
5. فرمان هوشمند ابتدا پیش‌نویس عملیات می‌سازد؛ عملیات مخرب بدون تأیید صریح اجرا نمی‌شوند.
6. گفت‌وگوهای فنی پروژه، مخاطبان محدود، نظرات و پیوست‌ها در `supabase/002_project_collaboration.sql` با دسترسی ردیفی تعریف شده‌اند.

## مسیر مهاجرت

1. ایجاد حساب و فضای کاری ابری.
2. آپلود یک‌باره داده `localStorage` و فایل‌های IndexedDB.
3. تطبیق شناسه‌های محلی با UUIDهای سرور.
4. فعال‌شدن همگام‌سازی و نگهداری صف آفلاین.
5. حفظ فایل پشتیبان محلی تا تأیید کامل مهاجرت.
